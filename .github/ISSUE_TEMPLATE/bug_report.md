---
name: Bug report
about: Report something that does not work as expected
title: "[Bug] "
labels: bug
---

**Describe the bug**
A clear and concise description of what the problem is.

**To reproduce**
Steps to reproduce the behaviour:
1. ...
2. ...

**Expected behaviour**
What you expected to happen.

**Environment**
- Distribution and version (e.g. Linux Mint 21, LMDE 6):
- Desktop environment (e.g. Cinnamon, GNOME, KDE, XFCE):
- Session type — run `echo $XDG_SESSION_TYPE` (must be `x11`; Wayland is not supported):
- cursorpop version (`cursorpop --version`):
- Installed from: `.deb` package / `make install` / running from source

**Logs**
If relevant, run the daemon from a terminal (`cursorpop`) and paste any output here.

**Additional context**
Anything else that might help (screenshots, GIFs, compositor in use, etc.).
